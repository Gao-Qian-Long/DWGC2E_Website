# 全站页面审查与二级页面统一 · 2026-09-15

基线：0d766b0；沿用纸色背景、墨色文字、橙色强调与现有导航结构。不改支付规则、Worker、数据库或密钥。

| 页面 | 本轮处理与验证 |
|---|---|
| index | 保留首页；FAQ、灯箱、移动导航、客服异常交互回归 |
| account | 保留账户布局；分项失败、会话失效、迟到响应、标签键盘回归 |
| billing | 保留套餐业务；隔离支付模拟、跨标签幂等、过期与恢复回归 |
| profile | 统一资料/密码双卡片；加载失败禁止空白覆盖，重试、草稿保护、改密后重新登录 |
| admin-feedback | 统一管理入口与列表；搜索筛选、分页保留、状态更新、401恢复、退出迟到响应 |
| devices | 统一卡片与导航；读取失败重试、撤销取消/成功、空状态 |
| history | 统一列表；首次失败不冒充空记录、重试和搜索无结果 |
| terminology | 统一表格与表单；保留存储失败、草稿导出、筛选选择回归 |
| translate | 移除无法完成上传的误导性界面，明确引导 Windows 客户端翻译 |
| status | 统一状态页，隐藏重复状态面板 |
| contact | 统一帮助页，与现有客服入口衔接 |
| updates | 统一更新说明页面 |
| privacy | 统一法律文档排版，不改条款业务含义 |
| terms | 统一法律文档排版，不改条款业务含义 |
| 404 | 统一错误页与返回入口 |

## 验证
- 15 项静态/API/代理测试通过（原14项 + 全站资源链接/ID/标题检查）。
- 原22项 UI 异常回归通过；新增13项二级页回归通过。
- 390px/1440px 基础交互通过；支付隔离测试首次桌面等待超时，独立重跑两宽度通过，保留该不稳定性记录。
- 15页 × 5宽度 = 75项溢出、可见标题、脚本检查通过。
- 390px/1440px 全站截图概览检查；资料/反馈非空与长内容截图检查；短视口客服回归。
- 证据位于 D:\DWGC2E\artifacts\site-review-20260915，未纳入网站发布文件。

## 边界
所有交互测试拦截业务接口，未访问真实管理员反馈、发邮件或付款。测试通过不代表真实支付/邮件业务验收。发布必须从本提交的干净归档使用白名单构建，预览验证后再发布；实际提交和部署结果另记于证据目录 RELEASE.md。

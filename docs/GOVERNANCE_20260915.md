# 2026-09-15 网站治理发布记录

## 已部署基础批次

- Worker 先行部署新会话模型和新空 D1：318abce7-a28d-480f-9692-51c7f7ca8fe2。
- 网站直接部署：6a49cad1.dwgc2e-website.pages.dev（Production/main）。后续修补通过 Git 构建发布，以 CF 实际部署记录为准。
- 用户授权测试数据重置：旧账号需要重新注册；网页不绑定 APP 安装实例。
- 页面风格与公开访问路径未改变。

## 已修复

- 网页独立登录和服务端退出；错误密码/非用户认证错误不误清已有账号。
- 请求取消、超时、换号迟到响应保护。
- APP 设备绑定查看/解绑及准确文案。
- 账户资料/当前密码改密契约；邮箱只读，改密后全会话失效。
- 匿名套餐可见、购买要求登录；待付款状态按服务端 user_id 分隔。
- “删除订单”改成“隐藏记录”，不暗示退款或取消平台订单。
- 本地术语按 user_id 隔离；“读取云端词库”不冒充双向云同步。
- 翻译历史接入实际文字用量记录、支持游标加载，不虚构在线 DWG 成品。
- 健康检查改用实际公共 health，状态使用 textContent，不将外部返回值作为 HTML。
- 管理反馈代理仅放行精确反馈路由/方法；真正的管理员权限仍由 Worker 独立校验，普通账号令牌不授权。管理页退出/离开后清除内存凭证，迟到请求不得重新显示记录。
- 删除 portal.js 内没有页面调用的旧支付入口/订单代码；保留独立 billing.js。
- 添加基础安全响应头；这不是宣称已完成完整严格 CSP/XSS 审计。

## 发布结构

根页面保留作为源码和本地预览入口；tools/stage-site.cjs 只将公开页面、js/css、assets 及公开元数据复制到 public。Functions 从根 functions 编译。内部 docs/tests/.git/.wrangler 不上传。

Cloudflare 项目构建命令为 `node tools/stage-site.cjs`、输出目录为 `public`。Git main 与上线源码同步。脚本要求空输出目录以阻止旧资源混入；在干净检出运行。不要使用旧 root-output 设置覆盖当前部署。

## 验证

- 请求/代理自动测试：14/14。
- 15 个页面 × 360/390/768/1024/1440：75 次无页面横向溢出、无 JS pageerror。
- 正式首页、账户、套餐在 390/1440 的只读浏览器检查；不提交注册、邮件或付款。
- 正式 health/plans=200，未登录 profile/devices=401；内部测试和文档 URL=404。
- 敏感日志与恢复基线：D:\DWGC2E_Baselines\20260915-102235。
- 截图与布局记录：D:\DWGC2E\artifacts\governance-20260915。

浏览器测试使用隔离模拟数据并拦截外站请求，不等于支付链路、验证码投递、管理员真实凭证或完整键盘操作验收。完整加载/失败/超时/并发状态矩阵仍有待补齐。

APP 新候选包尚未经过 AutoCAD 实机验收，现有公开下载未被替换。下载版本与真实发行包的完整核验仍未完成，不宣称本轮已发布新版 APP。

## 2026-09-15 继续治理：反馈交互回归

- 修复客服面板关闭后焦点错误返回浮动按钮的问题：现在返回实际打开面板的入口。
- 反馈增加请求中重复提交保护；迟到成功响应不清空用户新输入；缺少反馈编号的响应不显示成功、不清空草稿。
- 新增 tests/browser-interactions.cjs：390/1440px，FAQ 键盘、减少动态效果/暂停、灯箱焦点与背景恢复、移动菜单 Escape、客服焦点返回、重复提交/迟到响应/无效成功响应。所有网络隔离，仅模拟反馈，不发送邮件。
- 回归：14/14 请求/代理测试，75/75 页面宽度与脚本检查，新增两种宽度交互检查通过。
- 该批次只修改网页反馈行为，无 Worker、数据迁移或支付配置变更。准确部署版本以 APP 仓库 docs/ROLLOUT_RESULT_20260915.md 的追加记录为准。

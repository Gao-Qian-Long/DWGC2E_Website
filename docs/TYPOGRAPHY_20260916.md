# DWGC2E 网站字体可读性优化

日期：2026-09-16。范围：D:\DWGC2E_Website 网站源码；未修改桌面程序、翻译/回写算法、业务 JS 或线上数据，未部署。

## 审查

检查了全部 8 个 CSS 文件、16 个 HTML 页面及动态组件。记录了 184 条原有 ≤16px 的字体声明，其中 152 条 ≤13px（包括响应式重复规则，不能理解为152个组件）。原来无共享字号体系，只有颜色/字体族变量；账户根字号14px，导航11–13px，说明/表格/流程多为10–13px。先保存原CSS快照和逐选择器分类，再调整用途对应的规则，不做全局缩放。

完整原始声明及分类：D:\DWGC2E\artifacts\website-typography-20260916\font-audit.md。

## 共享 Typography Token

| 用途 | 原来 | 现在 |
|---|---|---|
| 主要正文、输入框 | 账户14px，其他14–16px | --text-body:16px |
| 卡片说明、导航、按钮、表格、表单提示 | 多为10–14px | --text-secondary:15px |
| 页脚链接、次要图注 | 7–13px | --text-small:14px |
| 简短状态徽标 | 11–12px | --text-badge:13px |
| 版权 | 11–12px | --text-copyright:12px |
| 英文装饰、节编号 | 9–12px | --text-decorative:11px |
| 流程标题 | 12–16px | --text-workflow:18px |
| 章节副标题 | 13–15px | --text-subtitle:18px |
| 正文行高 | 部分无统一规则 | --leading-body:1.75 |
| 次级说明行高 | 各处独立 | --leading-secondary:1.65 |

原有 Hero / 大标题 / 卡片标题不机械放大。保留品牌字体、颜色、图片、页面结构和版块顺序。深色章节的正文和说明适度提高对比度，保留原暖色体系。必要时允许自然换行；平板账户卡片仅微调内边距与链接间距，不降低字号。

## 修改文件和组件

- css/theme.css：语义字号变量、正文、按钮、链接、表单反馈、small。
- css/style.css：首页导航、说明、卡片、FAQ、下载、页脚、通用表格和页面；处理原有手机缩字号规则。
- css/drawing-board.css：产品说明、对比图说明、切换控件、工作流程、深色正文对比度。
- css/account.css：登录/注册/重置入口、账户侧栏、额度及设备卡片。
- css/secondary.css：二级页、设置、法律说明、后台反馈筛选/留言/操作。
- css/admin-users.css：用户列表、详情、筛选、分页、状态与历史说明。
- css/workspace.css：服务状态、客服表单、会员订单、动态组件、平板卡片间距。
- tests/typography-audit.cjs：新增本地隔离响应式截图/字号/溢出检查。默认全部尺寸；AUDIT_WIDTH可重验单尺寸并合并结果。

没有改动产品文案和业务接口。billing.css没有需要修改的字号，未产生实质差异。保留了本轮开始前已有的未提交修改。

## 仍然保留的小字号

- 11px：纯英文章节标题、MY ACCOUNT、WINDOWS APP、DWG × AI、票券编号、图组编号、平台版本元数据；装饰印章为aria-hidden。
- 12px：版权及其中分隔符。
- 13px：已登录、会员/工单状态徽标、管理员专用短标签。
- 含中文且承载信息的卡片标签、会员入口标签、手册标识已单独提升到14px，不再归为英文装饰。
- SVG工程线稿内部尺寸标记、宣传图片中的文字原样保留，不通过CSS放大图片内部文字。
- 移动端客服圆形图标仍用font-size:0隐藏原按钮视觉文字，保留原无障碍名称和图标；它不是阅读文字。

## 本地验证

Windows / Microsoft Edge headless、deviceScaleFactor=1，未使用zoom或缩放页面。全部API使用本地夹具，外部网络请求被拦截；不能将这些结果当成真实登录、邮件或支付验收。

| 视口 | 页面/状态检查 |
|---|---:|
| 1366×768 | 20 |
| 1440×900 | 20 |
| 1920×1080 | 20 |
| 2560×1440 | 20 |
| 768×1024（平板） | 20 |
| 390×844（手机） | 20 |

共120组：16个页面，外加账户登录、后台留言列表、用户列表与用户详情。保存240张首屏/整页截图；等待本地图像解码，避免懒加载图片在长截图中空白。

自动检查：页面横向溢出、直接文字垂直裁切、功能控件过小字号、浏览器脚本错误。无障碍隐藏文本和透明文件上传控件不按普通可见文字裁切处理。结果见 responsive.json；不能把DOM几何检查解释为所有视觉细节都已人工验收。

已有浏览器交互回归51项通过，含账户/术语库/后台留言/用户管理/图组键盘控制/模拟订单；API客户端、术语合并和页面链接检查22项通过。网站为静态HTML/CSS，无需或不存在单独的前端编译构建步骤。CSS差异空白检查通过。

人工抽查首页首屏、深色产品章节、手机后台留言、登录页、平板账户卡片；保留正常换行，不用小字号掩盖空间不足。全部截图供用户最终视觉验收。

## 查看证据

- 截图索引：D:\DWGC2E\artifacts\website-typography-20260916\index.html
- 全部截图：D:\DWGC2E\artifacts\website-typography-20260916\screenshots
- 响应式数据：上述截图目录的 responsive.json
- 测试日志：D:\DWGC2E\artifacts\website-typography-20260916\regression.log、static-tests.log、tablet-final.log

尚未发布网站；预览中的账户、留言、会员价格均为本地测试数据，不代表线上状态。实际设备上的主观阅读舒适度仍由用户最终确认。

# 后台一致性审查与字号整合 — 2026-09-16

## 审查基线与范围

等待“管理后台”任务完成后，依据其 `D:\DWGC2E\artifacts\plans-admin-20260916\DELIVERY.md` 审查当前合并工作区。该任务记录的 Pages 发布为 da6a67e0-7d4e-4fc7-928b-ced5b9101b40；本轮没有再次部署。

工作区仍含其他任务未提交的运营模块、会话、支付、云端工具及页面改动。本次只暂存审查相关 CSS、既有字号工作、Max 日期修复及测试/记录。尤其 style.css、workspace.css、admin-users.js 按内容拆分暂存，不整文件夹提交。测试针对当前合并工作区；不声称本次提交独立包含全部运营后台实现。

## 修改

- 整合正文 16px、次级说明/按钮/导航 15px、辅助文字 14px 的共享字号；保留标题层级与装饰小字，不做整体缩放。
- 十个后台模块共用暖米白输入框、细砂色边框、2px 克制圆角和棕橙焦点；替换新模块偏绿/纯白的视觉差异。
- 后台会话、导航、日志与表单接入共享字号；表单控件最小高度 44px。
- 手机端搜索、分页、会话说明允许换行；长反馈、错误及操作记录不撑破页面。
- 官网动态公告采用纸色、细线及棕橙侧边，不引入独立绿色卡片风格。
- 确认并修复 Max 到期时间提交为 null 的错误：Pro/Max 均提交按本机时区转换后的 ISO 日期，Free 保持 null。有效日期检查同时覆盖 Max。
- 匿名页面隔离测试允许精确公开端点 /api/v1/site（新运营功能的公开配置），继续禁止账户数据请求；未取消匿名隔离断言。

## 本地验证与证据

证据目录：`D:\DWGC2E\artifacts\admin-style-review-20260916`。

- typography.log：六种视口 390、768、1366、1440、1920、2560，共174组页面/状态检查，无横向溢出、直接文字裁切、过小功能控件或脚本异常。348张截图保存在 typography 子目录。
- review-final.log：2/2，通过390/1440下十个后台模块的布局、导航、字号、表单配色/圆角及失败恢复检查，另验证Max到期日期与保存失败输入保留。20张模块截图。max-before-fix.log记录原代码两种视口均提交null，修复后通过。
- operations-final.log：真实本地Worker + 内存SQLite集成2/2，包含登录、切页、配置、补偿和跨标签退出；不连接生产。
- unit.log：31/31。
- 已有浏览器回归：52项。首次47/52，5项为公开站点配置请求未纳入旧测试允许范围；更新精确允许列表。随后运行遇到本机ENOBUFS/ERR_ADDRESS_IN_USE和时序失败，保留日志，不将失败运行描述为全绿。分组复测auth-final.log为12/12，interactions-final.log为22/22；其余18项在regression-serial.log通过。
- 人工抽查：手机套餐、长反馈，桌面软件发布、公告配置、资料与安全页面。保持现有网站风格，没有重新设计全站。

只验证本地模拟接口和隔离数据。没有生产管理员写操作、支付、邮件、用户权益变更、数据库迁移或桌面构建。本次Git提交不等同于上线。
